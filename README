This project is a tool for constructing taxonomic ontologies (OBO format and OWL format using tax as individuals in progress).  In addition to the source and unit test code, the project contains an output ontology and several sample xml configuration files.